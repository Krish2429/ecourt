<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Case Details</title>
<style>
body {
  font-family: Arial, sans-serif;
  margin: 0;
  padding: 0;
  background-color: #f4f4f4;
}

.container {
  max-width: 600px;
  margin: 20px auto;
  padding: 20px;
  background-color: #fff;
  border-radius: 5px;
  box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
}

h2 {
  text-align: center;
}

.form-group {
  margin-bottom: 20px;
}

.label {
  display: block;
  font-weight: bold;
  margin-bottom: 5px;
}

.info {
  padding: 10px;
  border: 1px solid #ccc;
  border-radius: 5px;
}

.button-container {
  display: flex;
  justify-content: space-between;
}

.button {
  width: 48%; /* Adjust as needed */
  padding: 10px;
  background-color: #007bff;
  color: #fff;
  border: none;
  border-radius: 5px;
  cursor: pointer;
}

.button:hover {
  background-color: #0056b3;
}
</style>
</head>
<body>

<div class="container">
  <h2>Case Details Form</h2>
  <!-- Display client details -->
  <form action="/ecourt/ldashboard/accepted/<%= caseInfo._id %>/<%= lawyerId %>" method="post">
  <div class="form-group">
    <label class="label">Full Name:</label>
    <div class="info"><%= client.name %></div>
  </div>
  <div class="form-group">
    <label class="label">Email Address:</label>
    <div class="info"><%= client.email %></div>
  </div>
  <div class="form-group">
    <label class="label">Phone Number:</label>
    <div class="info"><%= client.contact %></div>
  </div>
  <div class="form-group">
    <label class="label">Address:</label>
    <div class="info"><%= caseInfo.address %></div>
  </div>
  <div class="form-group">
    <label class="label">Date of Birth:</label>
    <div class="info"><%= caseInfo.dob %></div>
  </div>
  <div class="form-group">
    <label class="label">Occupation:</label>
    <div class="info"><%= caseInfo.occupation %></div>
  </div>
  <div class="form-group">
    <label class="label">Gender:</label>
    <div class="info"><%= caseInfo.gender %></div>
  </div>
  <div class="form-group">
    <label class="label">Emergency Contact:</label>
    <div class="info"><%= caseInfo.emergencyContact %></div>
  </div>
  <div class="form-group">
    <label class="label">Legal History:</label>
    <div class="info"><%= caseInfo.legalHistory %></div>
  </div>
  <div class="form-group">
    <label class="label">Related Documents:</label>
    <div class="info"><%= caseInfo.relatedDocuments %></div>
    <!-- You can add the related documents here if needed -->
  </div>
  <div class="form-group">
    <label class="label">Case Description:</label>
    <div class="info"><%= caseInfo.caseDescription %></div>
    <!-- You can add the case description here if needed -->
  </div>
  <div class="form-group">
    <label class="label">Additional Comments:</label>
    <div class="info"><%= caseInfo.additionalComments %></div>
    <!-- You can add the additional comments here if needed -->
  </div>
  <!-- Accept and Decline buttons -->
  <div class="button-container">
    <button type="submit" class="button" name="action" value="decline">Decline</button>
    <button type="submit" class="button" name="action" value="accept">Accept</button>
  </div>
</form>
</div>

</body>
</html>
